# ==============================================================================
# 项目依赖包
# ==============================================================================

# 必需依赖
openai>=1.0.0              # OpenAI API 客户端，用于调用模型 API

# 推荐依赖（可选，但强烈建议安装）
tqdm>=4.64.0               # 进度条显示，提供更好的用户体验
                            # 如果不安装，程序仍可运行，但不会显示进度条

# ==============================================================================
# 安装说明
# ==============================================================================
# 安装所有依赖（包括推荐）：
#   pip install -r requirements.txt
#
# 仅安装必需依赖：
#   pip install openai>=1.0.0
#
# 安装推荐依赖（如果只安装了必需依赖）：
#   pip install tqdm>=4.64.0
# ==============================================================================
